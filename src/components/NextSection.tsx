/**
 * Placeholder white section directly under the hero. The body is intentionally
 * empty — real content for the next section is out of scope for this build.
 */
export default function NextSection() {
  return (
    <section
      aria-label="Below hero"
      style={{
        position: "relative",
        background: "var(--c-white)",
        minHeight: "60vh",
      }}
    />
  );
}
